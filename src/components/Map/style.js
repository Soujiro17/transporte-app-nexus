import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    overflow: "hidden",
    minHeight: 50,
  },
  map: {
    width: "100%",
  },
});

export default styles;
